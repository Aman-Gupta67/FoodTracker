NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Bypasses RLS entirely — never expose to the client, never
# prefix with NEXT_PUBLIC_. Required for phone-number login
# (src/app/api/auth/phone-login/route.ts). Find it in Supabase Dashboard →
# Settings → API → service_role key.
SUPABASE_SERVICE_ROLE_KEY=
